---
"@adjudicate/drift": minor
"@adjudicate/admin-sdk": minor
---

feat(drift): new @adjudicate/drift package — behavioral/statistical drift detection over the AuditEventBus (total-variation-distance, new-category, proportion-spike) with bounded cardinality and deterministic count-based windows (ADR-119).

feat(admin-sdk): add `governance.behavioralDrift` returning a drift snapshot for the console Behavioral Drift panel.
