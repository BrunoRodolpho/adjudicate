# apps/web — Round 2 high-density audit register

> Generated from the Phase-1 fan-out audit (6 read-only agents, 5 lenses: content · motion · visual · clarity · a11y). Severity: **P0** comprehension/conversion or broken · **P1** worth doing this round · **P2** polish. Round 2 commits to closing **all P0 + P1**; P2 is deferrable.

**Totals:** 128 findings across 6 clusters — P0 **1** · P1 **62** · P2 **65**.  By lens: motion 53 · content 36 · visual 18 · clarity 16 · a11y 5

> **Resolution — round 2 COMPLETE.** All **P0 (1) + P1 (62)** addressed: the **Home + Hero** cluster in Phase 3 (hero reposition closing the P0, MagicMoment, OutcomesBento, black-box reframe, motion kit + secondary-section content/motion); the **Console / Capabilities / Playground / Architecture / Transparency+Chrome** clusters in Phase 6 (chart-reveal motion, thin-page enrichment, page reveals, hover/stagger, a11y tooltips). Remaining **P2 (65)** are deferred polish. Verified: `build` (56 routes prerender) + **148 unit** + **35 e2e** green; invariants intact (no command text, no DB/Redis, illustrative/SIMULATED labels, generated counts); all motion reduced-motion-gated + transform/opacity-only.

## P0 — do first

- **Hero section headline and copy** _( clarity )_ — Headline 'A decision kernel for AI actions' is abstract without outcome framing. The subhead is strong (the 4-step sentence) but new visitors may not immediately connect the product to their problem.  
  ↳ **Fix:** Add 1-2 lines beneath the gradient-text headline that ground it in outcome: 'When your LLM takes action, you need visibility and control. Adjudicate sits between intent and execution—deciding, recording, and letting you audit every step.' This reframes from product category to business value.

## Home + Hero

_The homepage demonstrates solid technical architecture and clear narrative structure (the four-step spine is effective) but multiple surfaces are thin on copy depth and lack motion opportunities beyond the hero video. The Step sections show good visual hierarchy and examples (IntentEnvelope, receipt card), but supporting surfaces (Who It's For, Positioning, SocialProof, DepthLinks) are minimal. Most motion is gated behind the hero; secondary sections are static. Content richness is uneven: the spine steps have concrete examples; the positioning/audience sections have 1-2 sentences per card with no diagrams or elaboration._

| Sev | Lens | Surface | Issue | Fix |
|---|---|---|---|---|
| P0 | clarity | Hero section headline and copy | Headline 'A decision kernel for AI actions' is abstract without outcome framing. The subhead is strong (the 4-step sentence) but new visitors may not immediately connect the product to their problem. | Add 1-2 lines beneath the gradient-text headline that ground it in outcome: 'When your LLM takes action, you need visibility and control. Adjudicate sits between intent and execution—deciding, recording, and letting you audit every step.' This reframes from product category to business value. |
| P1 | clarity | SocialProof section subtitle clarity | Subtitle says numbers are counted from repo but doesn't explain why this matters. Readers see numbers but not the trust signal. | Expand: 'Every number below is counted directly from the codebase at build time—no estimates, no fiction. 150+ tests mean every decision path is exercised. 25 capabilities mean you can use any guarding logic. This is a real kernel, not a pitch.' Transparency + outcomes. |
| P1 | clarity | PlaygroundEntry teaser copy | 'Live · running real Packs server-side'—new visitor doesn't know what a Pack is. 'Live' label may be missed. | Clarify: 'Live playground: Run your own decision scenarios through the real kernel. Packs are pre-built flows (deployments, charges, KYC). Every decision lands in a live audit log.' Explains the value without assuming knowledge. |
| P1 | clarity | FinalCTA headline and subhead | Headline is generic. Subhead assumes reader knows they need this. New visitors may not connect to their problem. | Lead with outcome: 'Ship AI that users can trust. Every LLM decision is audited, every risky action is blocked or rewritten, every trace is provable.' Then show CTAs. Frames the benefit before asking for action. |
| P1 | content | StepActs envelope example copy | IntentEnvelope is shown but lacks plain-English context. Readers see dense JSON without explanation of why these fields matter. | Add 1-sentence summary above envelope: 'This is what an AI agent asks the kernel to do: deploy to production at full traffic, with no human approval recorded.' Makes the stakes clear before showing the raw data. |
| P1 | content | StepDecides 6-outcome chips | One-liners only. Visitors don't understand when each outcome is chosen or how ESCALATE differs from DEFER operationally. | Add hover or <details> disclosure expanding each chip with 2-3 lines: 'DEFER: Parks the intent until an external signal (webhook, queue message) arrives or timeout elapses.' Distinguishes outcomes clearly. Reduced-motion: use <details> instead of hover. |
| P1 | content | StepReceipt annotation text depth | Callouts are 1-2 sentences assuming crypto familiarity. New visitors skip them entirely. | Expand each to plain English: 'intentHash: A fingerprint of the proposed action. If one byte changes, the hash changes. Here: 9f4c…c4f2. This proves the action was not altered in flight.' Removes jargon, adds confidence. |
| P1 | content | WhoItsFor 4-card grid copy depth | Each card has title and 1-line value prop. No examples or use-case depth. Visitor doesn't know how to apply to their context. | Expand to 2-3 lines with concrete example: 'AI Platform Teams: Deploy a single guard across all models. Example: an HR LLM uses one Adjudicate instance to mediate employee-data queries—approve, rewrite, or escalate each.' Makes it tangible. |
| P1 | content | Positioning 3-card comparison grid | Each contrast is 1-2 sentences. No concrete differences in capability. No side-by-side feature table or diagram. | Expand each card with an example: 'vs Observability: Langfuse logs what happened after execution. Adjudicate intercepts before—so you can rewrite or block. Same deployment scenario: Langfuse sees the 100% ramp; Adjudicate prevents it.' Drives home outcome-first positioning. |
| P1 | content | GetStarted code example section | Three code blocks show examples but lack narrative. Copy is thin. New visitor doesn't understand conceptual flow. | Add intro sentence: 'Adjudicate runs in three lines of setup. Install the npm package. Define your first guard—a TypeScript function inspecting an envelope. Call adjudicate() to run it.' Add inline code comments highlighting outcomes ('// Returns REFUSE'). |
| P1 | content | DepthLinks 3-card signpost clarity | Three cards are minimal (title + audience + 1-line description). Visitor doesn't know when/why to visit each route. | Expand each: 'Architecture: For evaluators—Why a kernel sits between intent and side-effect. See the seven primitives, the decision algebra, and the audit trail model.' Add implicit use case. Consider visual breadcrumb hint ('Start here → read architecture → try introspection'). |
| P1 | motion | HeroKernelLoop video | 12-second loop is high-quality but renders alone with no annotation. Viewers watch but may miss the key moment (100% to 25% clamp) without rewatching. | Add optional animated caption bar beneath video (opacity fade-in, no layout shift) highlighting: 'Watches for the 100% ramp... clamps to 25%... audit saved.' Sync text fade-in to video timeline. Reduced-motion: show as static text overlay on poster. |
| P1 | motion | HeroOutcomeStrip 6-decision cards | Static grid with no hover interaction, reveal-on-scroll, or kinetic element. Cards are small on mobile and hard to scan. | Add staggered reveal-on-scroll using revealVariants + staggerContainer so each card enters sequentially. On hover: transform translateY(-4px) + subtle glow. Reduced-motion: static, no animation. |
| P1 | motion | StepReceipt annotation callouts | Receipt card + annotation rail are static. No reveal sequence. Callouts don't animate in as section scrolls into view. | Wrap receipt card in Reveal. Stagger annotation callouts in the rail: each fades + rises on scroll with 100ms spacing. Creates visual flow down the rail. Reduced-motion: all visible immediately. |
| P1 | motion | StepConsole audit explorer table | Static replica table. No row animation, no highlighting sequence. REWRITE row is highlighted but doesn't draw the eye. | Animate rows in from left on scroll: transform translateX(-20px) opacity 0 → translateX(0) opacity 1, staggered 60ms per row. REWRITE row pulses briefly (scale 1 → 1.05 → 1 over 600ms) after all rows enter. Reduced-motion: no animation. |
| P1 | motion | WhoItsFor card hover state | Cards are static. No hover lift, no color shift. Desktop users see no affordance that card is interactive. | Add hover: transform translateY(-6px), box-shadow increase, eyebrow text lightens (text-muted → text-ink). Transition 200ms ease-out. Reduced-motion: shadow only, no lift. |
| P1 | motion | SocialProof stat tiles kinetic animation | StatTile values (5 packages, 120 tests) are static. No counter animation, no kinetic reveal. Looks like a static badge. | Animate stat values on scroll-into-view: kinetic counter (0 → final value over 1s, easing out) fires once when stat scrolls into viewport. Use framer-motion `useMotionValue` for numeric animation. Reduced-motion: show final value immediately. |
| P1 | motion | GetStarted step cards reveal sequence | Three cards render statically. No reveal sequence, no code syntax highlighting animation, no visual flow from step 1→3. | Add staggered reveal on scroll (revealVariants, Reveal wrapper) with 120ms stagger per card. Draw subtle SVG arrow/connector beneath each card pointing to next step, fading in with the card. Reduced-motion: all visible, no animation. |
| P1 | motion | DepthLinks card icon animation on hover | Cards lift on hover but icon stays muted. Icon doesn't animate with the lift effect. | On hover: icon color shifts text-muted → semantic color (text-indigo-600). Icon scales 1 → 1.08 in sync with card lift. Reduced-motion: no lift or scale, just color change. |
| P1 | motion | FinalCTA button cluster animation | Three buttons render statically. No hover animation, no visual hierarchy, no kinetic draw to primary action. | Add hover states: GitHub button pulse-scale 1 → 1.02 + shadow. Playground button arrow slides right on hover (gap expands). Stagger button reveal on scroll (fade + rise, 80ms spacing). Reduced-motion: no animation. |
| P1 | motion | All secondary sections (WhoItsFor, Positioning, SocialProof, DepthLinks, GetStarted, FinalCTA) | Supporting sections do not use Reveal wrapper. Render immediately without fade-in. Creates visual discontinuity after hero's reveal flow. | Wrap all secondary sections in Reveal for visual consistency. Each section fades + rises on scroll using same revealVariants. Creates gentle cascade across entire homepage. Reduced-motion: all visible immediately. |
| P1 | visual | Positioning card visual hierarchy | Text-only cards on light canvas. Minimal hierarchy. No icons or color tinting. | Add semantic icon to each (Activity, Share2, Wrench). Tint each card with decision color at 5% opacity (EXECUTE for Observability, REFUSE for Gateways, ESCALATE for Middleware) to hint at outcome positioning. Icons add visual weight and scannability. |
| P2 | motion | PlaygroundEntry CTA button motion | Button is static. No hover animation or focus feedback. | Add hover: bg lightens, arrow slides right 4px (gap expands). On click: 100ms pulse-scale (1 → 0.98 → 1) for tactile feedback. Reduced-motion: color change only. |
| P2 | motion | ActiveStepStrip chip transition feedback | Chip highlight transitions smoothly but subtly. No visual feedback when active (no glow, no pulse). | On active state: scale 0.95 → 1 over 300ms, with shadow pulse (0 → 8px → 0 over 600ms, once). Reduced-motion: static highlight, no animation. |
| P2 | motion | Hero section scroll cue at bottom edge | No visual hint that more content awaits below. Outcome strip feels terminal. | Add animated chevron or arrow below outcome strip pulsing gently (opacity 0.5 → 1 → 0.5 over 2s loop). Or add tint overlay at hero bottom edge guiding eye downward. Reduced-motion: hide the cue entirely, rely on static design. |
| P2 | visual | Four Step sections visual richness and decorative elements | Sections have good contrast and hierarchy but lack visual richness beyond text and one artifact per section. No diagrams, no flow lines, no decorative elements. | Add subtle background SVG elements (opacity 2-5%) behind section titles: envelope shape for Step 1, decision tree silhouette for Step 2, hash symbol for Step 3, database icon for Step 4. Reinforces spine narrative without cluttering clarity. |

## Console replicas

_The console replicas (10 surfaces spanning audit-explorer, dashboard, drift, red-team, ai-bom, integrity, tokens, approvals, command-risk, and decision-detail) are technically sound, commitment-secure, and well-structured. However, many lack motion polish and surface-level explanatory depth—most render as static server components with minimal on-page copy. Round 2 should add reveal animations on charts, micro-interactions for interactivity feedback, and thin contextual hints (not copy, not labels) to surface intent faster._

| Sev | Lens | Surface | Issue | Fix |
|---|---|---|---|---|
| P1 | a11y | All replicas | Many custom tooltips and aria-labels carry technical jargon (TVD, ADR numbers, basis codes) without explanation. Tooltips are present but not always discoverable (title= attribute, not always surfaced). | For all aria-labels and tooltips, ensure they explain the 'why' in plain language, not just the 'what.' e.g., 'TVD (Total Variation Distance): a measure of how much your system's decision distribution has shifted' instead of 'TVD: 0.45.' Use title= sparingly; prefer aria-label for interactive elements. |
| P1 | clarity | /console/red-team | Pass/fail summary shows counts but lacks immediate clarity of what 'defended vs escaped' means for a first-time visitor. The run-history note 'selected run · 2026-05-XX · packId' is dense and cryptic. | Add tooltip on the pass/fail header: 'Defended = adversarial probe blocked, escaped = probe bypassed guard.' Keep it short (1 line). The run timestamp is already clear, but add pack-label legend if needed. |
| P1 | clarity | /console/command-risk | Category filter + disposition totals show counts but lack immediate clarity of what each disposition means. 'Blocked' vs 'Rewritten' vs 'Confirm' are not explained on-page. | Add title= tooltips on disposition summary stat headers: 'Blocked: command refused entirely. Rewritten: command modified before execution. Confirm: routed to human approval.' Use 1 line each. |
| P1 | content | /console/dashboard | StatTile counts are bare numbers with no contextual copy. 'Top refusals' section has no explainer of why these matter or what a refusal is. | Add 1-2 line tooltip or aria-description under 'Top refusals' header explaining these are the most common rejection codes—grounds for review. No label bloat; use title= attributes on code cells. |
| P1 | content | /console/ai-bom | BomDetail sections (Model, Conformance & health, Tools, Vector stores, Prompt hashes, Guardrails) are data-dense but lack introductory copy. A first-time visitor doesn't know why each section matters or what each field signals. | Add 1-line aria-description above each section header explaining its purpose: 'Model: the LLM powering this pack. Conformance: pass/fail + score on declared guardrails. Guardrails: safeguard rules active in this pack.' Keep it brief. |
| P1 | content | /console/approvals | Pending queue items show prompt, sessionId, time, and status, but lack context: what is REQUEST_CONFIRMATION? Why is review needed? Audit chain steps show timestamps + actor, but the 'claimed' label is buried in small print. | Add aria-label on pending item prompt: 'Awaiting human review—the AI system requested confirmation before executing this action.' Clarify on audit-chain that 'claimed actor' is not verified; emphasize via visual weight (not just small text). |
| P1 | content | /console (hub page) | Console hub groups replicas under 'Live feed', 'Analytics', 'Governance' with group-level descriptions, but individual replica cards have only a blurb (1-2 lines). No sense of which surface solves which problem for different operator personas. | Expand blurbs to 2-3 sentences (outcome-first): e.g., Audit Explorer → 'Every receipt, newest-first. See what your system adjudicated in real time and drill into full decision anatomy.' Keep it short, emphasize outcome (visibility → action). |
| P1 | motion | /console/dashboard | OutcomeChart (stacked area) renders as static SVG with no reveal-on-scroll or entrance animation. Chart is a key flagship visual but lands flat. | Wrap OutcomeChart in framer-motion with reveal animation: use revealVariants + whileInView on the SVG container. Animate stroke dasharray + opacity fade-in on paths (transform/opacity only, no viewBox shift). |
| P1 | motion | /console/drift | DriftReplica is interactive (chip selector, table, timeline chart) but TimelineChart renders static—no reveal-on-scroll or entrance animation. Charts are key visuals. | Wrap TimelineChart in framer-motion with reveal animation similar to Dashboard. Animate the chart's path + grid lines via transform/opacity. |
| P1 | motion | /console/red-team | RedTeamReplica has interactive pass/fail summary and run-history selector, but StackedAreaChart and BarDistribution render static—no entrance animation or chart reveal. | Wrap both charts in framer-motion with reveal animations (whileInView + revealVariants). Stagger chart elements via container/child variants on paths + bars. |
| P1 | motion | /console/tokens | TokenGovernanceReplica has interactive scope filter (All/Tenant/Session) and BurnBar visual indicators, but tables and timeline render static. BurnBar is a key visual signal but has no reveal animation. | Wrap tenant/session table rows and exhaustion timeline in framer-motion with staggered reveal. Animate BurnBar fill via scaleX (width-based scale, not layout shift). |
| P1 | motion | /console/ai-bom | AiBomExplorerReplica has two-pane layout with pack rail (interactive button list) + detail panel, but no reveal animation on either pane. Detail panel tables and sections render static. | Wrap detail sections in framer-motion with staggered reveal (container + children). Use motion-safe:transition-colors on rail items (already have hover, add motion entrance). |
| P1 | motion | /console/integrity | ConfigIntegrityReplica has interactive seal selection (table with button rows) + detail panel, but tables and TimelineChart render static. Timeline is a key visual but has no reveal animation. | Wrap seal table rows + detail panel in framer-motion with staggered reveal. Animate TimelineChart entrance via whileInView (chart path + grid). |
| P1 | motion | /console/command-risk | CommandRiskReplica has interactive category filter (All/Destructive/Credential/Network) + BarDistribution, but chart and blocked-commands table render static. BarDistribution is the primary visual and has no reveal animation. | Wrap BarDistribution in framer-motion with reveal animation (whileInView). Animate bar SVG paths + legend items via staggered container/child. Blocked-commands table should follow with staggered row reveals. |
| P1 | motion | All chart components (OutcomeChart, TimelineChart, BarDistribution, StackedAreaChart) | Charts across all replicas (dashboard, drift, red-team, command-risk) render as static SVG. No reveal-on-scroll, no entrance animation, no draw animation. These are key visuals but land flat. | Create a shared 'AnimatedChart' wrapper in lib/motion.ts or console-kit: accepts SVG element, applies framer-motion whileInView with staggered path/bar entrance (stroke-dasharray for lines, opacity scale for bars). Reuse across all charts. Transform/opacity only, no viewBox shift. |
| P2 | a11y | All replicas using motion-safe CSS | CommandRiskReplica and others use motion-safe: prefix correctly, but not all interactive transitions respect reduced-motion. Some button transitions hardcoded without the prefix. | Audit all transition- classes for motion-safe prefix. Any element with interactive feedback (button hover, filter select, state change) should be wrapped: motion-safe:transition-colors. Update consistently across all replicas. |
| P2 | clarity | /console/audit-explorer | Play/Pause toggle is clear, but first-time visitor doesn't know what 'SIMULATED' badge means or that records are committed sample data (only read the fine print in /console hub). | Add title= tooltip on the SIMULATED badge (already has one, line 38): 'This tail is a client-side simulation of committed sample data — not a live feed.' Ensure it's discoverable. |
| P2 | clarity | /console (hub page) | The data-flow link after 'committed sample data' is good for technical clarity, but the phrase 'committed sample data' itself may not land for non-technical visitors. No on-page explanation of why replicas exist or what they demonstrate. | Add 1-2 sentence paragraph below the video, before replica groups, explaining: 'These replicas mirror the real console's interface and workflows, driven by committed sample data so you can explore without a live system. The real console reads live operator data from Postgres + Redis.' Outcome-first, not too long. |
| P2 | content | /console/drift | Dimension selector and active-drift table lack introductory copy. A first-time visitor doesn't immediately grasp what TVD (Total Variation Distance) means or why elevated/high severity matters. | Add small aria-description on dimension selector group explaining TVD is a statistical distance measure; high TVD = behavior has shifted. Mirrors real console, already familiar to ops users. |
| P2 | content | /console/tokens | Scope filter and band labels (ok/near/over) are functional but lack guidance. A first-time visitor doesn't know why 'near' and 'over' matter or what threshold triggers each. | Add small aria-label on each BurnBar: 'tenant-id: XYZ of ABC tokens, 42%, near budget.' This is already done (line 402) but not exposed on table cells; ensure all table rows carry same level of detail. |
| P2 | content | /console/integrity | Active seals table columns (Pack, Status, Digest, Signature) and violations list are terse. Drift vs Sealed distinction is clear, but violation kinds (digest_mismatch, signature_failed, etc.) are not self-explanatory. | Violation kind labels are already translated (line 50–55 maps kind → label), but add small aria-label on violation badge explaining impact: 'Digest mismatch: the seal no longer matches the pack's content.' Keep it 1 line. |
| P2 | content | /console/decision/[intentHash] | HallucinationBadge shows score + bucket (grounded/uncertain/hallucinated) but lacks explanation of what the score means or why it matters. First-time visitor sees 'hallucination · score 0.34 · grounded' and doesn't understand the context. | Add aria-label on badge: 'Hallucination score (ADR-124): 0.34. Grounded = response well-supported by context. Score closer to 1.0 indicates risk of hallucination.' Keep it 2 lines, factual. |
| P2 | motion | /console/audit-explorer | AuditExplorerReplica has a solid simulated tail (useSimulatedTail with respects reduced-motion), and table rows highlight on arrival via transition-colors (P2, not P1—motion already exists). However, no entrance animation on the table container itself. | Add subtle reveal animation on the table container (opacity + y-slide) via whileInView. The row-highlight animation is already good; this is just the container entrance. |
| P2 | motion | /console/approvals | ApprovalCenterReplica has interactive tablist (Pending/Resolved/Audit) with proper keyboard nav, but tab panels render static—no entrance animation or stagger on list items. | Add reveal animation on the active tab panel (opacity + y-slide via whileInView). Stagger list items (pending queue, resolved history, audit chain steps) via container + child variants. |
| P2 | motion | /console/decision/[intentHash] | DecisionTraceReplica is server-rendered with optional HallucinationBadge + ReceiptCard (full variant with trace on). No entrance animation on badge or card. ReceiptCard likely has its own motion, but audit-specific surfaces lack reveal. | Wrap HallucinationBadge in framer-motion with reveal animation (whileInView). If ReceiptCard lacks motion, add reveal on its container. No layout shift, opacity + y-slide only. |
| P2 | motion | /console (hub page) | ConsolePage opens with an autoplaying video loop (console-tail.mp4) which is motion + sets tone well. Card grid for replicas renders static—no reveal-on-scroll. | Wrap REPLICA_GROUPS sections in framer-motion with staggerContainer + revealVariants on group headers + card containers. Video is already good; this is just card entrance polish. |
| P2 | motion | All interactive components (DriftReplica, RedTeamReplica, etc.) | Interactive components (selectors, tabs, filters) have hover transitions but lack subtle press/focus feedback. Button presses and state changes feel silent. | Add micro-interactions on interactive elements: scale-down on press (0.98 via initial/whileTap), fade-in on state change (filter/tab select). Use framer-motion's whileTap + smooth transitions. Reduced-motion safe via motion-safe: prefix. |
| P2 | visual | /console (hub page) | Console video loop is good, but replicas are listed in text cards without thumbnails or visual variety. The hierarchy is clear (groups + cards), but replica cards feel list-like rather than gallery-like. | Consider subtle background gradients or visual texture on replica cards (console-edge underlay, no hard change). Add decision-color left border (2–3px) to each card to signal the replica's primary concern (e.g., red for refuse-focused, emerald for conformance). Keep it subtle; don't add complexity. |

## Capabilities (/capabilities index + 14 [slug] pages)

_The capabilities cluster is technically complete—all 14 pages render fully via a single CapabilityPageLayout, tier markers are honest, and worked examples correctly isolate real-kernel runs from fixtures. However, Round 1 prioritized structural completeness over engagement: the index page is static, Tier-2 pages lack visual depth and enriched examples, and motion opportunities on reveals/transitions remain unmissed._

| Sev | Lens | Surface | Issue | Fix |
|---|---|---|---|---|
| P1 | content | Tier-2 capability pages (hallucination-scoring, ai-bom, red-team, behavioral-drift, config-integrity-seal, policy-coherence-analyzer, smart-approval-engine, agent-memory-store, incident-response-pack, access-governance-pack) | Tier-2 pages rely heavily on illustrative or reference examples (fixture receipts, pack intents, illustration cards) but lack outcome-first narrative and practical context. E.g. hallucination-scoring whatItDoes is 2 sentences (upstream observability), but does not explain: when to enable it, how it differs from tier-1 real-kernel, what thresholds operators set. The pages read as technical documentation rather than outcome-focused guides for the operator use case. | Expand whatItDoes on Tier-2 capabilities to include outcome framing: (1) what business/operator problem does this solve? (2) when should an operator reach for this? (3) how does it integrate with Tier-1 capabilities? For hallucination-scoring: 'Use this observability signal to gate releases when AI eval scores drop—it feeds release-gating thresholds without changing kernel decisions, making it a downstream risk gate.' For policy-coherence-analyzer: 'Run this design-time linter in CI before a policy ships to catch contradictions and dead rules—it surfaces structural issues no live test can find.' Keep sentences clear and operator-centric, avoid academic tone. |
| P1 | motion | /capabilities index grid | 14 capability cards render as a plain 3-column grid with no reveal animation. When the user scrolls to each family section, cards appear instantly rather than cascading in with the stagger pattern the homepage Reveal component establishes. This misses the cadence that makes depth pages feel polished. | Wrap each family's capability-card grid in the existing staggerContainer + revealVariants pattern (from lib/motion.ts). Each CapabilityCard gets variants={revealVariants}, and the parent <div> wrapping the grid maps initial="hidden" whileInView="visible" viewport={REVEAL_VIEWPORT}. Result: cards fade + rise as user scrolls, staggered by 80ms. Reduced-motion safe via the Reveal component wrapper pattern already established. |
| P1 | motion | /capabilities/[slug] page body blocks ("What it does", "Provenance", "Worked example", "Console") | Seven sequential section blocks (badges, step-2 note, what-it-does, provenance, worked-example, console, public-data) all render static on page load. No reveal, no stagger, no entrance transition. The capability name is already animated by DepthHeader (which is a static server component with no motion), so the body sections feel flat by contrast. | Wrap each <Block> in a client Reveal wrapper or apply revealVariants directly to the top-level flex container holding all blocks. Since CapabilityPageLayout is a server component, the simplest fix: add a thin "use client" wrapper around the flex-col gap-16 section, apply motion.div with variants={staggerContainer} initial="hidden" whileInView="visible", and give each <Block> section a wrapping div with variants={revealVariants}. Stagger delay 0.08s, no layout shift (opacity + y only). |
| P1 | visual | /capabilities/[slug] pages for illustration-kind examples (policy-coherence-analyzer, agent-memory-store) | Design-time capabilities (policy-coherence-analyzer, agent-memory-store) render a plain text block in an IllustrationExample card. No diagram, no visual anchor, no structure beyond a prose paragraph. The illustration body for policy-coherence-analyzer is 80+ words of dense text ('rules that can never fire, outcomes shadowed...'); agent-memory-store's illustration is similar. These deserve a visual scaffold (e.g., a simplified state flow diagram or labeled box diagram) to make the concept memorable. | For policy-coherence-analyzer: add a simple three-step diagram (Policy Bundle → Static Lint → Diagnostics) with 2-3 example diagnostics listed below (e.g., 'Rule shadow: Phase 1 REFUSE shadows Phase 2 REWRITE'). For agent-memory-store: add a simple labeled box showing (Envelope + State S) → Kernel vs (Memory M) → Prompt only, to visually reinforce that M is upstream-only. Use CSS or minimal inline SVG; keep diagrams under 250px height. No motion needed; the visual structure alone reduces cognitive load. |
| P2 | a11y | /capabilities index page and /capabilities/[slug] pages | The capabilities pages (both index and detail) have no explicit reduced-motion handling for the proposed reveal animations. If motion is added to the index grid or page-body blocks, a user with prefers-reduced-motion:reduce should see all content immediately, not faded in. Currently, DepthHeader and CapabilityPageLayout are server components with no motion, so they are already safe; but if motion is added (as in finding P1), reduced-motion middleware must be present. | For any motion additions (grid reveals, page-body block staggers), wrap the animated container in the existing Reveal client component (from src/components/home/Reveal.tsx), which already respects useReducedMotion() and degrades to a plain <div> under reduced-motion. Alternatively, for the CapabilityPageLayout page-body blocks, create a thin "use client" AnimatedBlocksContainer that applies motion.div with whileInView logic and includes useReducedMotion() check. Pattern: if reduce, render children in a plain <div>; otherwise, use staggerContainer + revealVariants. |
| P2 | clarity | /capabilities/[slug] DepthHeader + page back link | DepthHeader has a back link ('Back to capabilities') that is text-muted + text-xs, blending into the header background. On a fast scroll to the page, the back link is easy to miss. The header title + subtitle are strong (text-3xl / md:text-4xl), but the affordance to return is subtle and low-contrast. This is a UX affordance issue, not a content one, but it affects first-time navigation clarity. | Boost the back link visual weight slightly: change text-muted to text-ink/80 (slightly darker, higher contrast). Keep the icon and 'Back to capabilities' label, but add a subtle hover:text-ink state so the link appears interactive. No layout change; just Tailwind color tweaks to DepthHeader className. |
| P2 | clarity | /capabilities index page subtitle + family blurbs | The page subtitle explains the Tier badge ('Tier 1 runs the real kernel or a live projection; Tier 2 is fixture-illustrative'), but a first-time visitor may not understand the operational significance—why Tier 1 matters more, or whether Tier 2 is 'safe' to use. The four family blurbs are terse one-liners ('Keep classified data and ungrounded output out of side-effects') that are jargon-dense for someone new to adjudicate. | Expand family blurbs by 1-2 words each to add outcome framing. E.g., 'Content & data safety' blurb: change from 'Keep classified data and ungrounded output out of side-effects' to 'Prevent classified data and hallucinated facts from reaching external systems—unclassified payloads and grounded logic pass through.' Alternatively, keep the short version on the index and add an expandable <details> or hover tooltip that shows the expanded version. This respects the 'inevitable, not encyclopedic' homepage discipline while adding clarity on demand. |
| P2 | clarity | /capabilities/[slug] all Tier-1 and Tier-2 pages | The step-2 context note ('This is what happens at Step 2 — Guard decides...') is a helpful anchor, but it assumes the user has seen the homepage and understands the four-step model. A visitor landing on a capability page directly (via search, share link, or depth page jump) may not have context for what Step 2 means. The note is present but doesn't link to /how-it-works or provide a quick visual reminder of the four steps. | Add a single inline link in the step-2 note: 'This is what happens at Step 2 — Guard decides (see the four-step model on the homepage) for ...' The link text 'see the four-step model on the homepage' should link to '/#step-acts' or '/how-it-works'. This is a one-line fix that bridges users who land mid-flow. Alternatively, add a small 'Learn more about the kernel model' text-link at the end of the step-2 callout. No component change needed; just edit the static text in CapabilityPageLayout. |
| P2 | content | Tier-1 live-kernel worked examples (pii-guard, command-risk-guard, token-budget-guard, release-gating) | Live-kernel examples show a real ReceiptCard (decision + trace) but lack narrative context. A first-time visitor sees an EXECUTE + basis codes, but may not understand why this specific payload matters for the capability. E.g., pii-guard shows 'My SSN is 123-45-6789'—a toy example that is immediately obvious; token-budget-guard shows a search tool on a session with 6000 consumed tokens—no explanation of what the budget cap is or whether 6000 is 'at risk'. The worked examples are technically correct but don't teach the capability's scope. | Add a one-line caption above each live-kernel ReceiptCard explaining what the example demonstrates. For pii-guard: 'This payload contains classified data (SSN + card number); the kernel masks it and returns a REWRITE so the action completes safely.' For token-budget-guard: 'With 6000 tokens consumed against a 7500-token session budget, this agent step is approved—but is near ceiling.' For command-risk-guard: 'A pipe-to-shell request requires confirmation; the kernel never echoes the raw command, only reports the risk category.' These captions sit just above the ReceiptCard and mirror the pattern used in the red-team and drift worked examples (the ConsoleChrome caption). |
| P2 | content | Tier-2 chart examples (red-team, behavioral-drift) in worked examples | Red-team and behavioral-drift render public transparency charts (BarDistribution, TimelineChart) wrapped in ConsoleChrome, but the sample data legends and axis labels are minimal. Red-team shows 'Scenarios defended per pack' but does not label what the bar values mean (count? percentage?). Drift shows a TimelineChart with 'T-6' to 'T-0' buckets but no explanation of the time window or why severity is 'elevated'. These are illustrative samples, so adding annotations would clarify intent without claiming live accuracy. | For red-team: add a small info callout or footnote ('Each bar shows the count of adversarial scenarios blocked by that pack\'s policies') below the BarDistribution. For drift: expand the severity / active alerts row to include a one-line note ('Elevated: drift distance rising; 3 alerts firing on outcome distribution shift'). Keep callouts short (1 line) and use the existing Callout tone="info" component to stay consistent. |
| P2 | content | /capabilities/[slug] Tier-2 pack examples (incident-response-pack, access-governance-pack) | Pack examples list the governed intents (e.g., 'incident.remediation.execute') and outcome chips (DEFER, REWRITE, ESCALATE) with an illustrative note ('not a live run'). But a user new to packs doesn't learn what a pack is conceptually or why incident-response or access-governance are the exemplars. The intents are raw identifiers with no glossing—someone unfamiliar with the domain (e.g., an infra team lead considering adjudicate) sees opaque names and has to click to the ADR to understand scope. | Add a small sentence above the intents list explaining the pack's jurisdiction. E.g., for incident-response-pack: 'This pack governs AI-driven incident remediation—defining who can trigger a fix, what blast radius is safe, and when to escalate.' For access-governance-pack: 'This pack controls access requests—approving least-privilege grants, escalating sensitive resources, and gating destructive revokes.' One sentence per pack, text-sm, text-muted. This primes the user to understand why those specific intents matter. |
| P2 | content | /capabilities/[slug] all pages (Tier 1 and Tier 2 alike) | The "What it does" section is mechanism-first (how the capability works) but rarely frames operator wins or failure modes. E.g., hallucination-scoring whatItDoes explains the scorer is 'upstream-only' and 'informs dashboards,' but does not say what happens if an operator ignores a hallucination alert or what signal thresholds to configure. For Tier-1 real-kernel capabilities, this is less critical (the worked example speaks for itself); for Tier-2, it is a gap. | For Tier-2 capabilities only, add a single sentence at the end of whatItDoes that frames the operator action. E.g., hallucination-scoring: add '...which is why operators typically gate releases or escalate reviews when scores drop below a configured threshold.' For policy-coherence-analyzer: add '...letting teams fix the bundle before a policy reaches production.' These sentences are outcome-first and unify Tier-2 pages around the operator use case, preparing them for full enrichment in Round 3. |
| P2 | content | /capabilities/[slug] all Tier-1 pages | Tier-1 capabilities are marketed as 'Shipped · real-kernel,' which sets a high bar for understanding scope. However, 'shipped' is vague: does it mean production-ready in adjudicate's own deployments? Operator-configurable? The oneLiner and whatItDoes are clear on mechanism, but a skeptical operator may wonder 'can I actually use this today, or is it a preview?' This is not a problem with the copy itself but with missing context about shipping stage. | Add a sentence in the step-2 context note specifically for Tier-1 pages: '(This is a Tier 1 capability—real-kernel, shipped with adjudicate, fully configurable in your deployment.)' This reassures the operator that the capability is not theoretical. For Tier-2, adjust the note to: '(This is a Tier 2 capability—illustrative, available as a reference, with fixture examples on this page.)' Alternatively, add this context to the maturityBadge tooltip or as a small info icon next to the badge. One-line change in CapabilityPageLayout, conditional on tier. |
| P2 | motion | /capabilities index page and /capabilities/[slug] detail pages | Hover states exist on cards (Card component has hover:shadow-md + hover:border-ink/30) and links, but there is no micro-feedback on click or focus. The capabilities grid is large (14 cards × 3-4 variants), and a user clicking through cards expects some visual confirmation that the navigation occurred. Currently, the browser's default navigation is the only signal. | Add a subtle click feedback: when a CapabilityCard is clicked, it could briefly scale down (1 → 0.98) with transition-transform duration-150 (or similar). This is micro and doesn't require framer-motion; pure Tailwind active: state and transitions. Alternatively, ensure that the Link component from next/link preserves tabindex and has clear focus:ring states (already in the Card component's hover styles, but add focus:ring-2 focus:ring-offset-2 for keyboard nav). This is a polish tweak, not a Round 2 blocker, but improves perceived responsiveness. |
| P2 | visual | /capabilities/[slug] page layout and whitespace | Each capability page stretches to ~1800px before wrapping, with dense text blocks (max-w-3xl), creating visual weight. The gap-16 between sections is generous (4rem), but within each section the typography and spacing feel compressed. Headings (text-xl) sit directly above subtitle text (text-sm) with only mt-1, creating visual ambiguity about hierarchy. The DepthHeader is separated from the body Section by a pt-10, but the visual connection between title and content could be clearer. | Increase visual breathing room: (1) Boost h2 subtitle spacing from mt-1 to mt-2.5; (2) add a subtle background tint to the step-2 context note (currently has bg-surface + border, but could use a softer tint like bg-defer/3 to signal importance without loudness); (3) ensure the Block <section> headings all use a consistent border-b treatment and padding so the scanning experience is uniform. No code change needed—pure Tailwind spacing/color adjustments in CapabilityPageLayout.tsx. |
| P2 | visual | /capabilities/[slug] ProvenanceCard (ADR + package links) | The ProvenanceCard layout (icon + label on one row, primary code below, secondary code below that) works, but the secondary code (e.g., 'packages/primitives/src/guards.ts') is text-[11px] text-faint—very small and hard to scan. When both cards are side-by-side on desktop, the visual weight is unbalanced: the primary code is text-base font-semibold (bold, prominent), but the secondary path is tiny and muted, making it feel like an afterthought rather than a verifiable claim. | Increase secondary code readability: change text-[11px] to text-xs (a more readable size), keep text-faint for color. This small bump makes paths scannable without adding layout shift. Alternatively, add a subtle code background (e.g., bg-canvas or bg-defer/5) to the secondary text block to create visual grouping. No semantic change; pure presentation. |
| P2 | visual | /capabilities/[slug] "How it appears in the console" section | The "How it appears in the console" section is a plain flex box with text + a link button. The text is terse (e.g., 'PII Events drill-down + public PII-handling posture'), and there's no visual anchor (no icon, no preview, no badge) to show the operator what surface they'll see. The button 'Open the console replica' has a border, but the whole panel feels like a dead end rather than a gateway to live instrumentation. | Add a small icon (e.g., lucide Window or ArrowRight) to the left of the 'Open the console replica' button to signify navigation. Optionally, add a subtle bg tint (e.g., bg-defer/3 or bg-execute/3) to the entire section to visually separate it from surrounding content, signaling 'this is where you go next.' These are pure visual tweaks that require no component restructuring. |
| P2 | visual | /capabilities/[slug] ReceiptCard rendering for Tier-1 real-kernel examples | ReceiptCard renders the full audit record (decision + basis + trace + payload diff), which is correct and transparent. However, the card is content-dense (many sections, nested <details> for progressive disclosure), and on mobile or narrow viewports, the layout can feel cramped. The card variant="full" is well-suited for a worked example (showing 'the real kernel did X'), but there is no visual hierarchy between 'payload' (input) and 'rewrite' (output) sections. | In ReceiptCard rendering for capability pages only, consider adding subtle visual separation: a light border or background tint between the payload section and the decision outcome section (e.g., a border-t or py-2 gap). This helps a user scanning the card quickly identify 'input → kernel → output.' No content change; pure spacing/styling via Tailwind. This is a Polish P2, not a content gap. |
| P2 | visual | /capabilities/[slug] page body — overall flow and pacing | The page follows a consistent structure (badges → step-2 note → what-it-does → provenance → worked-example → console → public-data) that is logically sound. However, visually, the pacing feels even: each section is a <Block> with the same heading style (text-xl), and there is no visual hierarchy suggesting which sections are 'primary' vs 'supporting.' The worked-example is conceptually the centerpiece, but it has the same visual weight as 'Provenance' or 'Public data.' | Subtle visual hierarchy tweak: make the 'Worked example' heading slightly larger (text-2xl) and optionally add a thin border-l (accent color, e.g., border-execute or border-defer) to signal importance. Keep the content structure identical, but use Tailwind to visual-weight the worked-example block. Alternatively, add a subtle background tint (e.g., bg-surface with a slightly warmer tone) to the worked-example section to make it pop. These are micro-tweaks that require no component restructuring. |
| P2 | visual | /capabilities index page family sections | The four family sections are separated by gap-16 (4rem) and each has a border-b divider line with a brief family blurb below. The visual distinction between families is clear, but there is no color coding, icon, or visual badge to quickly scan which family you are in. A user scrolling through all 14 capabilities sees four repeated patterns (border + heading + blurb + 3-4 cards), which feels flat. | Add an optional small icon or color accent next to each family heading to create visual variety. E.g., 'Content & data safety' → shield icon (or a small color pill, e.g., bg-refuse/10), 'Adversarial & behavioral' → alert icon, etc. Keep icons small (16px) and muted so they don't overwhelm the heading. Alternatively, add a subtle left border accent (e.g., border-l-4 border-defer) to each family section container, with a different color per family. This is a visual polish that aids scanning without changing the content or layout. |
| P2 | visual | /capabilities/[slug] "Public data" section (when transparencyRoute exists) | The public-data link section is a simple card with a heading, route code, and an ArrowUpRight icon. It is functional but bland—no description of what the public view shows, no indication of data freshness or scope (e.g., 'illustrative sample only' vs 'live aggregate'). | Add a one-line subtitle below the 'Public transparency view' heading explaining what the link shows. E.g., for pii-guard: 'Aggregate PII-event counts and redaction rates—sample data, no tenant detail.' For red-team: 'Defense posture trends per pack—live counts, illustrative scenarios.' Keep text-sm and text-muted so it doesn't overwhelm the card. This improves clarity and primes the user for what they will see on the transparency page. |
| P2 | visual | /capabilities index page and /capabilities/[slug] detail pages | All text content is left-aligned and full-width (max-w-6xl container), with no right-margin visual anchors or sidebars. This is clean and scannable, but on ultra-wide displays (2560px+), the text stretches to uncomfortable line lengths (>90 characters). The DepthHeader subtitle spans max-w-2xl, but the body blocks only use max-w-3xl, creating an asymmetric reading experience. | Ensure all text blocks in CapabilityPageLayout use consistent max-width constraints. The whatItDoes should probably be max-w-3xl (already is); the Block wrapper should inherit max-w-3xl or max-w-2xl to keep reading line length under 75-80 chars. This is a small Tailwind tweak that improves readability on wide displays without affecting mobile or tablet. |

## Playground

_The Playground cluster is technically sound and functionally complete, but content is thin in places (Sandbox onboarding, Receipt density), motion is almost entirely absent (major miss for guided step transitions and result reveal), and visual repetition in ReceiptCard makes long receipts hard to scan. Round 2 should animate step progressions, lighten receipt hierarchy, and add outcome-first value copy to Sandbox._

| Sev | Lens | Surface | Issue | Fix |
|---|---|---|---|---|
| P1 | content | /playground — Sandbox initial state (SandboxResult) | The empty result panel is a generic EmptyState ('No run yet') with no value prop copy. Sandbox is positioned as 'Configure & test' but the initial hint doesn't explain *why* you'd use it (advanced testing, custom payloads, debugging) or what the outcome is worth. | Replace EmptyState hint with outcome-first copy: 'Configure a Pack and intent, edit the payload, and run the real kernel. The receipt below will show every decision step, guard match, and signature—ideal for testing edge cases or debugging policy rules.' This mirrors the Guided→Sandbox bridge in Playground.tsx subtitle. |
| P1 | content | /playground — Receipt decision explanation (ReceiptCard DecisionSection) | The DECISION section shows the outcome chip + a reason or message, but doesn't explain *what happened* in plain English for a first-time visitor. For REWRITE, it shows a before/after JSON diff but no summary sentence like 'The kernel lowered the ramp percentage from 100% to 25% to reduce risk.' For DEFER, it shows signal + timeout but no explanation of what 'deferred' means in the context of this Pack. | After the outcome chip, add a one-sentence plain-English summary: (1) EXECUTE: 'The kernel approved the request as-is.' (2) REFUSE: 'The kernel blocked the request.' (3) REWRITE: 'The kernel approved a modified version.' (4) DEFER: 'The kernel paused the request pending a signal.' (5) ESCALATE: 'The kernel routed the request to [human/queue] for review.' (6) REQUEST_CONFIRMATION: 'The kernel asks the caller to confirm.' This is the *first thing* a visitor reads about the outcome. |
| P1 | motion | /playground — Guided step result reveal (GuidedStep) | When a step runs, the result region (outcome chip + narrative + receipt toggle) appears instantly with no entrance animation. The receipt is also non-animated—it's just present or hidden. This loses the 'materialization' moment that makes the kernel decision feel earned. | On result arrival, fade + slide the result region in (opacity 0→1, y 12→0, 0.4s ease-out via revealVariants). When the user toggles the receipt open, slide-down the ReceiptCard with a slight delay (0.1s, opacity + transform only). Wrap in motion.div and respect prefers-reduced-motion via useReducedMotion hook. |
| P1 | motion | /playground — Guided step progression (StepStrip in GuidedCaseRunner) | The StepStrip shows 'AI acts → Guard decides → Receipt saved → Console shows' but is completely static. It doesn't animate the active phase as the step runs, so visitors don't see the narrative spine *progress* with their actions. The phase stays on 1 until result, then jumps to 3—no 'in-flight' feeling. | Animate the active step indicator in StepStrip: when busy/running, transition the active phase from 1→2 (Guard decides) on fetch start, then to 3 (Receipt saved) on result. Use a transform-only animation (no layout shift). Add a subtle pending pulse or color shift during phase 2 to signal 'computing'. Respect motion-reduce. |
| P1 | motion | /playground — Sandbox result reveal (SandboxResult) | When a run completes, the ReceiptCard appears instantly. The error alert also appears instantly. No entrance animation, no layering—just a static render. This is a missed moment: the receipt is the *point* of the run. | Wrap the ReceiptCard and error alert in motion.div with revealVariants (opacity 0→1, y 12→0, 0.4s ease-out). Stagger them if both appear. Respect motion-reduce via useReducedMotion. Ensure the spinner animation is still gated (already is, per code). |
| P1 | visual | /playground — Receipt card density (ReceiptCard) | ReceiptCard is feature-complete but DENSE. Every section uses text-[10px], has a monospace font, is collapsible, and sits in a tight grid. The INPUT, TRACE, DECISION, BASIS, AUDIT, CRYPTOGRAPHIC sections all look identical (mono text in details tags). For a newcomer or on a small screen, a full receipt is overwhelming and hard to scan. Visual hierarchy is flat. | Tier the receipt for visual hierarchy: (1) Header stays unchanged (decision + time). (2) INPUT/TRACE sections: keep collapsible but boost heading to text-[11px], tighten padding slightly (px-3 py-2.5). (3) DECISION section: keep visible, enlarge relevant text (outcome kind to text-[13px] bold, reason to text-[12px] italic), use slight color tint. (4) BASIS/AUDIT/CRYPTO: keep collapsible, use smaller headings. This preserves all detail but makes scanning faster. |
| P2 | a11y | /playground — Receipt accessibility & perf (ReceiptCard) | ReceiptCard uses native <details> for disclosure (good), but doesn't announce section state to screen readers. The JSON segment view is a large <pre> block with monospace text—hard for SR users. No skip links for long receipts. Trace table uses aria-hidden on the border-left indicator, which is correct, but phase labels are bare text. | Add aria-label to each details element: 'Expand INPUT section', etc. For JSON segments, add a brief aria-label before the <pre> block: 'Request envelope (JSON)'. Consider a summary toggle at the top of the receipt: 'Show simplified view' that collapses all details and shows only the DECISION section. Ensure colour isn't the only differentiator in trace (already uses text colour + border, good). |
| P2 | clarity | /playground — ConsoleHandoff bridge (ConsoleHandoff.tsx) | The 'This receipt would now appear in your operator console' messaging is good, but the component appears the same way on every step/run (same copy, same styling). It feels like boilerplate, not a value-driven CTA. Visitors may skip it. | Add context-aware copy: in Guided mode, after the first step, show 'Receipts like this populate your operator console. Open the Audit Explorer to see them in the live feed.' After the final step, upgrade to 'All steps above produced signed receipts. View them together in the Audit Explorer.' In Sandbox, always show the generic version. Use a flag/prop to distinguish the context. |
| P2 | clarity | /playground — Main section heading (Playground.tsx) | The title 'Try it — the kernel below is real.' is punchy but doesn't lead with outcome ('See live decisions' or 'Watch the kernel decide'). The subtitle explains that receipts are real and signed, but doesn't frame the Guided/Sandbox split as 'safe default + power-user escape hatch.' | Reframe the title to outcome-first: 'Watch the kernel decide' (lead with what the visitor *sees*). Expand subtitle to clarify the mode split: 'Every decision runs the real Packs server-side and produces a signed receipt. Start guided to see a complete business story, or jump to sandbox to configure your own test. Real kernel, real time.' |
| P2 | content | /playground — Guided entry (GuidedCasePicker) | Intro copy is thin — 'Pick a real business situation...' is generic and doesn't explain why guided mode is safer/faster than jumping straight to Sandbox. No example screenshots or benefit callouts. | Expand intro paragraph to 2–3 sentences positioning Guided as the 'no-code' onboarding: 'Pick a real business situation from an installed Pack. Each story runs the real kernel step-by-step—no JSON, no setup. Best for first-time visitors; jump to Sandbox when you're ready to configure.' |
| P2 | content | /playground — Guided step narrative (GuidedStep + guided-narration.ts) | The 'whatToWatch' lines in GuidedStep are helpful but brief (one line, often under 60 chars). The result region shows only the outcome chip + one-liner; no diagram or visual explanation of *why* the kernel made that choice (what guard fired, what rule applied, etc.). | Extend the 'Guard fired' narrative: instead of just 'The token-budget rule made the call.', add a one-sentence plain-English explanation of that rule's job (e.g., 'It checks if the request would exceed the account's monthly budget.'). Reuse/extend the basis explanations already in content/basis-explanations.ts. |
| P2 | content | /playground — Sandbox Pack/Intent picker (SandboxPackPicker) | The picker shows Pack names + an intent dropdown but no explanation of what each Pack is for or what the intents do. Copy is purely structural ('1 · Pick a pack', '2 · Pick an intent'). Newcomer doesn't know which Pack to choose. | Add a small subtitle above the Pack picker: 'Choose an installed Pack (e.g., Payments, Deployments) and the intent kind you want to test.' After intent selection, show a one-line hint under the dropdown describing that intent (e.g., 'pix.charge.refund: Process refund requests for Pix charges'). Source hints from SANDBOX_SCHEMAS. |
| P2 | motion | /playground — Collapsed step list (GuidedCaseRunner + GuidedStep) | Earlier steps collapse from active to a one-line chip with no animation. The list just re-renders, which feels abrupt. Upcoming steps are hidden (hidden attr) and appear with no stagger when reached. | Animate collapsed step chips: on collapse, fade out + slight scale-down (0.3s). When advancing to a new step, fade + slide the new active step in (revealVariants, 0.4s). Use staggerContainer on the ol wrapper to stagger child entries as they mount. Respect motion-reduce. |
| P2 | visual | /playground — Receipt trace table (ReceiptCard TraceSection) | The trace is rendered as a vertical list of rows (phase label, guard name, outcome) with colour-coded borders. Dense, monospace, hard to scan which phase/guard matched. Newcomer sees 'default[2]' and 'business[1]' labels and doesn't understand the flow. | Add lightweight diagram context: replace the raw phase label ('default[2]') with a small badge ('phase: default' in a secondary style). Show a thin vertical line linking matched rows to emphasize causality. Consider a horizontal timeline view for longer traces (1-line per step, left-to-right), but preserve monospace detail toggle. Keep it transform/opacity-only. |
| P2 | visual | /playground — Playground tab segment styling (Playground.tsx) | The tab control is functional and accessible but visually underdeveloped. The 'Start here' / 'Configure & test' hints are small pills that don't stand out. The descriptions (one line per tab) are buried in the button text. No hover state on the unselected tab to signal it's clickable. | Boost visual weight: (1) Make selected tab transition more prominent (add a subtle shadow or glow on transition). (2) Lift the hint pill slightly above the label (re-order with flex-col-reverse on unselected, normal on selected). (3) On hover of unselected tab, lighten the background to surface-light and darken the text slightly. (4) Consider adding an icon to each tab (e.g., play for Guided, sliders for Sandbox) to aid scanning. |
| P2 | visual | /playground — Sandbox empty form state (SchemaForm) | When a user first selects a Pack/intent, the SchemaForm renders all fields with default values (often empty strings or 0). For a first-time user, this is a blank canvas with no guidance on what to enter or what outcomes to expect. No example or tooltip hint visible by default. | Add inline hints or placeholders to each form field: use the `title` from the schema (if available) or a domain-specific hint (e.g., 'E.g., ch_synthetic_1' for a chargeId field). For numeric fields, show the valid range. If space allows, add a 'Try this example' button that populates the form with a pre-built scenario (one per intent), then show the outcome without forcing a user to invent a payload. |

## Architecture + trust + deploy

_The cluster is technically sound with strong copy on deploy and architecture pages, but lacks motion throughout. DataFlowDiagram and TrustBoundaryPanel are particularly static given their visual complexity, and the Comparisons page is thin on copy depth and entirely un-animated. GuardMetadataGraph stands out with excellent hover interactions._

| Sev | Lens | Surface | Issue | Fix |
|---|---|---|---|---|
| P1 | clarity | /introspection | Page header is sparse (3 lines) and immediately jumps to a complex radial graph. Visitors unfamiliar with GuardMetadata or ADR-105 have no on-ramp. The guardrails concept (threshold, state_defer, system_taint, rewrite, opaque) is named but not explained inline. | Expand the DepthHeader subtitle by 50-75 words to include: (1) plain-English definition of GuardMetadata ('structured labels that describe each policy rule'), (2) why it matters ('analyzers and auditors can introspect your policy programmatically, not as a black box'), (3) the five description kinds and one real-world use case (e.g., 'a threshold guard says "allow if amount < cap" and its metadata tags that cap value and the reason'). Inline code examples are optional but recommended. |
| P1 | content | /comparisons | Page is minimal (36 lines). Only mounts two sections (DecisionsGrid + WedgeTable) with a shallow DepthHeader. No explanatory preamble, no real-world examples of OPA/Cedar limitations, no illustrative diagrams showing when DEFER/REWRITE matter vs allow/deny. | Add a 200-word preamble section (post-header, pre-grid) explaining: (1) why permission engines are insufficient for AI workflows, (2) a concrete scenario (e.g., 'an LLM wants to transfer $10k but policy caps it at $5k') that shows allow/deny vs REWRITE, (3) a visual: two-column side-by-side diagram showing OPA decision tree vs adjudicate six-outcome tree. Reuse Decision colors from DECISIONS tokens. Can be a simple flex layout with icons, no new deps. |
| P1 | motion | /architecture/data-flow · DataFlowDiagram | Complex multi-stage diagram (6 nodes across 5 columns, 2 branches, terminus) rendered as static HTML/CSS grid. No reveal-on-scroll, no animated connectors, no step-wise drawing. Missed opportunity: this is the most visually dense artifact in the cluster. | Wrap PipelineNode components in motion.div with staggered reveal-on-scroll (whileInView with REVEAL_VIEWPORT). Add opacity fade to FlowEdge arrows + staggers to OutcomeChips for visual progression from left to right. Transform-only (opacity + y-offset); no layout shift. |
| P1 | motion | /architecture/data-flow · TrustBoundaryPanel | Comparison table (2 columns × 4 rows) is entirely static. No reveal-on-scroll, no row-by-row stagger, no hover lift. The boundary concept is abstract; motion could make the contrast between console and web perceptible. | Wrap table rows in staggered motion.div (whileInView reveal with delay per row). Add subtle hover:shadow and hover:-translate-y-0.5 to each row. Keep transform/opacity-only for reduced-motion safety. |
| P1 | motion | /comparisons · WedgeTable | Three-column table (OPA/Cedar, adjudicate, Capability) is completely static with no reveal-on-scroll or row stagger. 8 comparison rows; no visual rhythm or progression. Table headers and rows load all at once. | Wrap table rows in staggered motion.div with whileInView reveal (0.4s duration, 0.06s stagger). Ensure thead is visible immediately (no delay). Add group-hover:-translate-y-0.5 to rows for interactivity. Respects REVEAL_VIEWPORT pattern. |
| P2 | content | /deploy | Copy is solid and examples are concrete, but the Audit persistence section (lines 142–165) could benefit from a real scenario. The audit flow (kernel → record → Postgres → SSE → console) is named but not illustrated with a concrete example trace (e.g., 'When the kernel DEFERs, here's what lands in the audit_intent table'). | Add a small inline code example after the metadata.jsonb explanation showing a sample row (or a JSON snippet) from the partitioned intent_audit table for a DEFER outcome. Keeps the example platform-neutral (e.g., '{ decision: "DEFER", intentHash: "...", metadata: { reason: "awaiting_payment_confirmation" } }'). Link to the console section for the end-to-end flow. Minimal add; high clarity gain. |
| P2 | motion | /introspection · ConsolePreview | Static dark-band section advertising the operator console. No reveal-on-scroll, no animated console mockup (rows could scroll/draw in). Visual richness is low; mockup is a simple div with static HTML. | Wrap the mockup rows (6 audit-explorer rows) in staggered motion.div (whileInView with 0.05s stagger per row). Add subtle opacity fade + x-offset (-20px) on entries so they 'flow in' from left. For reduced-motion safety, ensure CSS classes exist for static state. Optional: add a subtle pulse/glow to the most recent row (lime) to suggest 'live'. |
| P2 | motion | /architecture · Problem section | Animation quality is good (arrow wobble, phase opacity pulse), but the animation is decorative rather than demonstrative. The phases (state, taint, auth, business) blink in sequence, but there's no visual explanation of what each phase does or how they compose into the kernel. | This is a nice-to-have: add a tooltip or micro-interaction (on hover of a phase badge) showing a one-liner of what that phase gates. E.g., hover 'taint' → show 'Validates actor provenance' in a small popover. Alternatively, keep current animation but make it smoother: increase stagger delay to 0.6s (currently 0.4s per phase), hold each phase visible 0.8s before dimming. Reduced-motion-safe: just show all phases at opacity-1 without animation. |
| P2 | motion | /architecture · PrimitivesDiagram | Reveal-on-scroll is present and working, but no additional richness. Cards load as a simple staggered reveal (no scale, no rotation, no color shift on hover beyond border change). | This is already solid; keep as-is. Optional polish: add a subtle scale shift on hover (scale: 1.02 on group-hover) to complement the existing border color lift. Respects reduced-motion by virtue of using motion.a's whileInView pattern (will be honored by framer-motion). |
| P2 | motion | /comparisons · DecisionsGrid | Already good: reveal-on-scroll + hover lift. No critical issues. | Surfaces is already strong. Minor polish: ensure hover:-translate-y-1 is paired with a subtle shadow increase (group-hover:shadow-md exists; good). No changes needed. |
| P2 | motion | /introspection · GuardMetadataGraph | No critical issues. SVG circles have good hover scale (1.4x) and cursor feedback. Reduced-motion-safe (no explicit check, but framer-motion honors it by default in newer versions). | Surface is already strong. Optional: document that prefers-reduced-motion will disable the whileHover scale; add an explicit useReducedMotion check if stricter a11y is desired (but current framer-motion behavior is acceptable). No code change required. |
| P2 | visual | /architecture/data-flow · StepStrip + overall page flow | The page has strong visual hierarchy (header → step strip → pipeline → trust boundary → CTA) but the pipeline diagram is horizontally dense on mobile and the trust boundary table stacks into a tall vertical block. Visual balance could be improved with better spacing or a collapsible view for smaller viewports. | Minor: ensure lg:grid-cols-[...] breakpoints on DataFlowDiagram and TrustBoundaryPanel match the page's max-width (6xl). Verify mobile rendering doesn't cause excessive horizontal scroll. Optional: add a subtle background gradient or card shadows to separate the two diagram sections. No functional change; CSS refinement only. |

## Transparency + Blog + Global Chrome

_The transparency hub is architecturally solid and well-documented, but individual transparency pages are data-heavy yet visually and interactionally thin. The single blog post is lean and launch-focused. Global chrome (NavBar, SiteFooter) is functional and well-structured but lacks motion polish. Round 2 should enrich transparency pages with outcome-first narrative, interactive data visualization, and reveal animations; expand the blog with post-launch content; and add subtle motion to global navigation._

| Sev | Lens | Surface | Issue | Fix |
|---|---|---|---|---|
| P1 | clarity | Global chrome (NavBar + SiteFooter) + transparency pages | First-time users landing on a transparency page won't immediately understand the outcome/business value of that surface. Each page leads with technical description ('aggregates only', 'never raw data') rather than outcomes ('see how often we refuse destructive commands'). Trust is built on clarity, not compliance disclaimers. | Restructure each transparency sub-page header: put a 1-line user-outcome headline first ('See what we refuse in the wild' for command-risk; 'Is configuration drifting?' for drift), then technical description. The current h1 becomes an h2 under an outcome-focused h1. This reorders narrative priority without removing compliance language. |
| P1 | content | /transparency/pii | Thin narrative — page reads like a bare audit report. No real-world context for why PII handling matters or what the counts mean to a user. Missing: example scenarios (e.g., 'when a user asks for a refund'), guidance on sensitivity classes, or comparison to baseline best practices. | Add a 2–3 sentence outcome-first intro explaining the business value: 'Sensitive fields can't be refused wholesale — they can only be redacted or blocked. Here's how often each class goes through each path.' Include a visual example row with colored sensitivity-class badges and real-world interpretation (e.g., 'Email addresses: redacted 92% to block phishing leaks'). |
| P1 | content | /transparency/ai-bom | BOM card is a dense 2-column grid of mostly-empty cells. Lacks narrative connective tissue: why does a user care about conformance vs. health vs. guardrails? No visual hierarchy or section grouping. When a cell is empty, the 'Not declared' placeholder reads as an absence, not a feature. | Reorganize BomCard into sections (Model & Conformance, Intent/Signal/Basis, Guardrails & Tools, Manifest Hashes). Add outcome-first headers: 'Conformance & Health' → 'Does this pack defend against EU AI Act requirements?' with a 1-line explainer. Use a semantic color code for health tiers (teal/amber/red badges, not plain text). Include a small SVG legend or callout showing what each section controls (e.g., 'Guardrails = the rules the kernel enforces'). |
| P1 | content | /transparency/drift | Drift status is presented as two static badges (severity + top dimension) with no explanation of what drift *is* or why it matters. A first-time visitor won't understand: what would they do if they saw 'High' severity? Why is 'intent kind' a top dimension? Missing narrative that bridges the compliance/technical divide. | Add a visual explainer section before the status badge: 'Behavioral drift = the agent's decisions are shifting away from expected patterns. A sudden decision-distribution change signals either a model update, a new pack version, or a governance issue. The severity band tells you how urgent it is.' Include a simple before/after comparison diagram (don't expose raw TVD; just show: 'decision kind distribution shifted 12%'). |
| P1 | content | /transparency/red-team | Red-team defense cards show only two numbers (defended/total) and a binary badge (Clean/Regressed) with no narrative context. A user won't know: what counts as a 'good' defense percentage? Are there baseline expectations? How frequently are these runs executed? What do the attack scenarios test? | Add an intro paragraph: 'Each pack is stress-tested against a known adversarial suite (prompt injection, taint escalation, tool-scope attacks). These cards show whether the defenses held in the most recent run. A regressed pack needs immediate investigation; a clean pack passed all scenarios.' Link to a documentation anchor explaining the adversarial test taxonomy (without revealing payloads). |
| P1 | content | /transparency/tokens | Token burn-down shows a single aggregate percentage with no historical context or trend line. A user won't know: is 68% good? Is it trending up or down? What happens at 100%? Missing guidance on what token budgets control and why they matter. | Add a 2-sentence outcome-first explainer: 'Token budgets cap the total tokens an AI deployment can consume in a period. When you're near the limit, the kernel starts deferring or rejecting new actions. This page shows where you stand.' Add a simple context bar: 'Typical SaaS deployment: 50–70% by mid-period. Enterprise: 30–40%.' (Illustrative, no real data.) |
| P1 | content | /transparency/integrity | Configuration integrity page shows a seal-verification badge and a kill-switch stability band, but lacks narrative connective tissue. A user won't know: why does a seal matter? What does 'Storm' stability mean operationally? What triggers a kill switch? Missing outcome-first framing. | Add an intro section: 'Each pack's configuration is cryptographically sealed — a digest proves nothing changed since deployment. The kill-switch stability class tracks how often the emergency stop was triggered: stable = routine, recurring = concerning, storm = critical. All three signal different kinds of governance health.' Include a simple state-transition diagram (SVG, not interactive): Stable → Single Incident → Recurring → Storm. |
| P1 | content | /blog (index page) | Blog has only one post (88 lines). Page reads like a placeholder. No editorial voice, no thematic coherence, no content roadmap visible to users. Visitors will see 'Notes from the kernel' as aspirational, not established. | Add an editorial header (3–4 sentences) below the h1: 'We write about kernel design, decision governance, and lessons from shipping open-source policy. Posts range from technical deep-dives to operational case studies. Check back for launch post-mortems, threat-model updates, and design philosophy.' This sets expectations and invites return visits. |
| P1 | content | /blog/[slug] (post template) | The single post 'Launching adjudicate' is launch-focused and technical but reads as sparse: no introduction section with context for a non-expert, no conclusion or forward-looking statement, no related-posts or discussion prompt at the end. Post metadata is minimal (only date, author, summary visible in index). | Add a front-matter intro (2–3 sentences) above the article that situates the post for different audiences: 'If you're evaluating adjudicate: jump to [link]. If you're curious about the kernel design: start here. If you're shipping v0.2: check v0.2 roadmap at the end.' Add a footer CTA: 'What's next?' with links to architecture docs, GitHub issues, or the next post in the series. |
| P1 | motion | /transparency/pii + /transparency/command-risk | Static tables with zero interaction or reveal animation. Bar charts render at page load with no stagger, fade, or entry animation. Bars themselves are static inline divs with no hover interaction or delayed fill. | Wrap the table rows in a framer-motion staggerContainer (via a new client component TableRows). Each row fades + rises on scroll via revealVariants. On each row's bar div, add a reduced-motion-safe fill animation on mount: initial `width: 0` → final `width: ${widthPct}%` over 0.6s with EASE_OUT (no transform flicker, width is acceptable for bars). Add subtle hover brightness/opacity lift on table rows. |
| P1 | visual | /transparency (hub page) | The 'Public views' grid is a 3-column card layout with minimal visual hierarchy. All seven cards are equal weight; no visual categorization by risk/governance domain. No icons, badges, or visual signifiers to help users quickly scan and find the relevant surface. | Add a small semantic icon to each card's header (e.g., shield for red-team, dollar-sign for tokens, circuit-board for ai-bom). Group the cards into two sections visually: 'Risk & Compliance' (red-team, drift, integrity) vs. 'Operations' (PII, command-risk, tokens, AI-BOM). Use a subtle background tint or left-border color per group, not a line break. |
| P2 | a11y | src/components/ui/NavBar.tsx + SiteFooter.tsx | Mobile sheet uses framer-motion with reduce logic, but desktop dropdown does not. A11y is good (aria-expanded, aria-haspopup, aria-current), but animation consistency could be better. No explicit motion-safe defaults in NavBar's DesktopDropdown. | Import useReducedMotion in DesktopDropdown and apply it to the panel's motion.div animation (same pattern as MobileSheet). Ensure keyboard users (Tab, Escape) experience the same smooth transitions as mouse users, not instant state changes. |
| P2 | clarity | src/components/ui/NavBar.tsx global header | The wordmark includes a version badge (v0.1) that's visually small and easy to miss. On a dense header with many links, there's no visual focal point or hierarchy to guide the eye to the product name. | Keep the wordmark but increase the visual prominence slightly (font-semibold is good; maybe add a subtle background glow or increase letter-spacing). Consider adding a visual badge (e.g., 'experimental' or a beta pill) next to v0.1 to communicate the stage more clearly without needing text. |
| P2 | motion | /transparency/ai-bom BomCard sections | BOM cards appear all at once; no entrance animation or progressive disclosure. The grid of cells (Model, Intents, Tools, etc.) is visually flat despite semantic grouping. | Wrap BomCard in a motion.article with revealVariants (fade + rise on scroll). Inside, group cells by section and wrap each section's title + cells in a motion.div with a slight stagger (0.05s per section). No layout-shift animations — only opacity and y-offset. |
| P2 | motion | /transparency/drift badge + status indicator | Status badge (with Activity/TrendingUp icon) renders statically. Icon choice (green vs. amber) conveys urgency, but there's no visual pulse or emphasis to draw the eye. 'What this does not show' callout appears static below. | Add a gentle pulse animation to the icon (opacity 1 → 0.6 → 1, 2.4s loop) only when severity is 'elevated' or 'high'. Respect prefers-reduced-motion by degrading to no animation. The callout section should fade + rise in from below via revealVariants when scrolled into view. |
| P2 | motion | /transparency/red-team DefenseCard grid | Defense cards render in a static 2-column grid with no stagger or entrance animation. Shield icons (ShieldCheck/ShieldAlert) don't animate; the visual distinction relies on icon + color alone, but no motion draws attention to a regressed pack. | Render the ul as motion.ul with staggerContainer. Each DefenseCard wraps in motion.li with revealVariants. When a card's status is 'Regressed', add a subtle 2px horizontal bounce (scaleX: 1 → 1.02 → 1, 0.4s) on mount (only once) to signal urgency, respecting prefers-reduced-motion. |
| P2 | motion | /transparency hub card links | Cards have only a static hover:border-ink/30 transition. No scale, shadow, or interactive depth cue. The 'View →' text is static. | Add a motion.div wrapper to each card body. On hover, apply a subtle scale (1 → 1.02, 100ms, no layout shift via transform), plus a slight y-offset on the 'View →' text. Group the cards in the ul as motion.ul with staggerContainer; wrap each li in motion.li with revealVariants. |
| P2 | motion | /transparency/tokens burn bar | Burn-bar fill is static. No animation on page load, no response to user interaction. The percentage label is static text. A kinetic reveal would make the burn rate visually impactful. | Animate the fill on mount: width 0 → ${pctUsed}% over 1s with EASE_OUT, using transform: scaleX origin-left to avoid layout shift. Pair it with a counter that animates from 0 → ${pctUsed} (tabular-nums). Respect prefers-reduced-motion by rendering to final state instantly. |
| P2 | motion | /transparency/integrity badge + stability indicator | Seal badge and stability band are static. No visual feedback on the stability progression (Stable is clearly good, but Storm's urgency isn't emphasized). Icon (ShieldCheck/ShieldAlert) doesn't animate. | Apply revealVariants fade + rise to the entire badge on scroll. When stability is 'storm' or 'recurring_incidents', add a subtle pulse to the border color (opacity 0.4 → 0.8 → 0.4, 1.6s loop), respecting prefers-reduced-motion. Add a simple animated state-transition diagram (SVG with stroke-dasharray reveal on scroll). |
| P2 | motion | /blog post card | Blog post cards in the list render statically. No entrance animation, no hover state beyond default link styling. The 'Read post →' CTA is a plain link with no visual emphasis. | Wrap the ul as motion.ul with staggerContainer. Each li becomes motion.li with revealVariants. On hover, the card's border shifts to ink (not just 30% opacity), and the arrow animates right (x: 0 → 2px, 200ms). The entire card has a subtle shadow lift. |
| P2 | motion | src/app/layout.tsx global page transition | No page transition animation between routes. Layouts change instantly with zero visual continuity or crossfade. Global chrome (NavBar, SiteFooter) stays mounted, but the content area has no motion on navigation. | Create a PageTransitionWrapper client component that wraps {children} in a motion.div with initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} (0.3s, EASE_OUT). Use AnimatePresence with mode='wait' on the Providers wrapper. Respects prefers-reduced-motion by rendering to final state. |
| P2 | motion | src/components/ui/NavBar.tsx desktop navigation | Desktop dropdown menu transitions use only opacity and visibility (no transform), so the panel appears to jump. Chevron rotates but the panel doesn't scale or slide. | Change the dropdown panel div from transition-opacity to motion.div with initial={{ opacity: 0, y: -8 }} animate={{ opacity: open ? 1 : 0, y: open ? 0 : -8 }} transition={{ duration: 0.18, ease: EASE_OUT }}. Respects prefers-reduced-motion via the existing reduce flag. |
| P2 | motion | src/components/ui/SiteFooter.tsx | Footer is a static server component with no entrance animation or visual hierarchy progression. Decision chips at the bottom are rendered as a flat list with no stagger. | Convert the footer link columns and decision chips to a client wrapper component. Animate each footer column (fade + rise via revealVariants on scroll). Stagger the six decision chips on mount: each chip fades + scales from 0.95 → 1.0 over 0.4s with 0.06s stagger, respecting prefers-reduced-motion. |
| P2 | motion | /transparency/[surface] headers (all 7 pages) | Each transparency sub-page header (h1 + p) renders statically. No entrance animation or progressive disclosure, despite these being key landing zones for users navigating from the hub. | Wrap the header in a motion.header with initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.5, ease: EASE_OUT }} on page load (not on scroll). This gives each page a gentle entrance without relying on scroll-triggered reveals. |
